fastapi
uvicorn[standard]
python-multipart
pydantic-settings
boto3
psycopg[binary]
psycopg_pool
pgvector
numpy
opencv-python-headless
insightface
onnxruntime

# --- Moderación de contenido (NSFW + gore), todo ONNX, SIN PyTorch ---
nudenet           # detección de desnudez (ONNX) -> rechazo de NSFW
transformers      # CLIP processor/tokenizer para zero-shot de gore (no requiere torch)
huggingface_hub   # descarga de los pesos ONNX de CLIP
Pillow            # preprocesamiento de imagen para CLIP

# Auth de admin: hash de password (bcrypt) + JWT firmado.
# bcrypt<4.1: passlib 1.7.4 lee un atributo interno que se renombró en 4.1+.
passlib[bcrypt]<1.8
bcrypt<4.1
PyJWT>=2.8,<3

# --- Testing (dev) ---
pytest>=7.0
httpx>=0.24
pytest-asyncio>=0.21
pytest-cov>=4.0
requests
